@model LCCS_School_Parent_Communication_System.viewModels.EvidenceApprovalViewModel
@{
   
}

    @*@using (Html.BeginForm("EvidenceApproval", "ULHome", FormMethod.Post, new { @enctype = "multipart/form-data" }))
        {
            <input type="file" name="file" />
            <input type="submit" name="submit" value="Upload" />
        }*@



    @using (Html.BeginForm("EvidenceApproval", "ULHome", FormMethod.Post))
    {
        @Html.HiddenFor(h=>h.Id)
        @Html.ListBoxFor(l => l.days, new SelectList(Model.days),new { style= "max-width:550px; height:300px; border-color:blue;font-size:1.5em; " })
        <input type="submit" name="approve" value="Approve" class="btn btn-primary" />
    }


    @if (ViewBag.view)
    {
        var base1 = Convert.ToBase64String(Model.document);
        var src = string.Format("data:application/pdf;base64,{0}", base1);
        <iframe src="@src" height="800"style="width="100%" ></iframe>
    }


<script type="text/javascript">

    $(function () {
        $('#days').multiselect({
            includeSelectAllOption: true
        });
    });

</script>