@model LCCS_School_Parent_Communication_System.viewModels.announcementViewModel
@{
    ViewBag.Title = "announcementManagement";
}
<style>
    .side {
        display: inline-block
    }

    .container-fluid {
        width: 85%
    }
</style>


<script>
    $(document).ready(function () {
        $('#tblAbsenceRecord').DataTable();
        $('#tblAbsenceRecord2').DataTable();

    });
</script>




<div class="container mt-5 bg-light border-top border-bottom border-4 rounded-3">
    <h1 class="border-bottom border-5 p-3 mb-4">Grade List</h1>
    <div class="row">
        <div class="col-12">
            <div class="row">
                <table class="table table-striped table-bordered" id="tblAbsenceRecord2">
                    <thead>
                        <tr>
                            <th>Grade</th>
                            <th>Academic Year</th>
                            <th>Number of Sections</th>
                            <th>Number of Students</th>
                            <th>
                                @using (Html.BeginForm("addFullAnnouncement", "ADHome", FormMethod.Get))
                                {

                                    <input type="submit" name="add" value="Add Full Announcement" class="btn btn-primary" />

                                }
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var k in Model.gradeList)
                        {
                            <tr>
                                <td>@k.grade</td>
                                <td>@k.academicYear</td>
                                <td>@k.numberoFSections</td>
                                <td>@k.numberOfStudents</td>
                                <td>
                                    @using (Html.BeginForm("addAnnouncement", "ADHome", new { anId = k.grade.ToString() }, FormMethod.Get))
                                    {

                                        <input type="submit" name="add" value="Add Announcement" class="btn btn-primary" />
                                        <input type="text" name="txtAddAnnouncement" value=@k.grade style="display:none">
                                        <input type="text" name="txtAddAnnouncement1" value=@k.academicYear style="display:none">
                                    }

                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    @*@if (ViewBag.successfulMessage != " " && ViewBag.successfulMessage != null)
        {
            <div class="alert alert-success">
                @ViewBag.successfulMessage
            </div>
        }
        @if (ViewBag.MessageStatus != " " && ViewBag.MessageStatus != null)
        {
            <div class="alert alert-danger">
                @ViewBag.MessageStatus
            </div>
        }*@
</div>


<div class="container mt-5 bg-light border-top border-bottom border-4 rounded-3">
    <h1 class="border-bottom border-5 p-3 mb-4">Announcment List</h1>
    <div class="row">
        <div class="col-12">
            <div class="row">
                <table class="table table-striped table-bordered" id="tblAbsenceRecord">
                    <thead>
                        <tr>

                            <th>Announcement Type</th>
                            <th>Title</th>
                            <th>Content</th>
                            <th>Post Date</th>
                            <th>End Date</th>
                            <th>FileName</th>
                            <th>Actions</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var k in Model.announcementList)
                        {
                            <tr>

                                <td>@k.announcementType</td>
                                <td>@k.announcementTitle</td>
                                <td>@k.announcementContent</td>
                                <td>@k.postDate.ToShortDateString()</td>
                                <td>@k.endDate.ToShortDateString()</td>
                                <td>@k.filName</td>
                                <td>
                                    <div class="side">
                                        @using (Html.BeginForm("updateAnnouncement", "ADHome", new { annId = k.announcementID.ToString() }, FormMethod.Get))
                                        {

                                            <input type="submit" name="update" value="Update Announcement" class="btn btn-primary" />
                                            <input type="text" name="txtUpdateAnnouncement" value=@k.announcementID style="display:none">
                                        }
                                    </div>
                                    <div class="side">
                                        @using (Html.BeginForm("announcementManagement", "ADHome", new { annId = k.announcementID.ToString() }, FormMethod.Post))
                                        {

                                            <input type="submit" name="delete" value="Delete" class="btn btn-danger" />

                                        }
                                    </div>

                                </td>

                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    @if (ViewBag.SuccessfulMessage != " " && ViewBag.SuccessfulMessage != null)
    {
        <div class="alert alert-success">
            @ViewBag.SuccessfulMessage
        </div>
    }
    @*
        @if (ViewBag.MessageStatus != " " && ViewBag.MessageStatus != null)
        {
            <div class="alert alert-danger">
                @ViewBag.MessageStatus
            </div>
        }*@
</div>
