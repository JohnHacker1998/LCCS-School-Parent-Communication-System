@model List<LCCS_School_Parent_Communication_System.Models.Assignment>
@{
    ViewBag.Title = "viewAssignment";
    double m;
}


@*
<table class="table table-striped table-bordered">
    <tr>
        <th scope="col">Assignment Type</th>
        <th scope="col">Assignment</th>
    </tr>
    
        @foreach (var k in Model)
        {


<tr>
    <td>@k.assignmentType</td>
    @using (Html.BeginForm("viewAssignmentDetails", "PHome", new { idk = k.assignmentId.ToString() }, FormMethod.Post))
    {
    <td><input type="submit" value=@k.assignmentType /></td>
    }
</tr>
        }
       
</table>*@
    <div class="container mt-5 bg-white border-bottom border-white border-4 rounded-3">
        <div class="row title-button">
            <span class="p-2 title col-md-10">Assignments</span>
        </div>
        @if (Model.Count() > 0)
        {
            <div class="list-group mt-1 mb-2 shadow">

                @foreach (var k in Model)
                {

                    using (Html.BeginForm("viewAssignmentDetails", "PHome", new { idk = k.assignmentId.ToString() }, FormMethod.Post))
                    {
                @Html.AntiForgeryToken()
                <button class="list-group-item list-group-item-action align-items-start  mt-2 btn btn-primary">
                    <div class="d-flex w-100 justify-content-between pe-2">
                        <h5 class="mb-1">@k.assignmentName</h5>
                        <small>
                            @k.submissionDate.ToShortDateString()

                        </small>



                    </div>



                    <div class="d-flex w-100 justify-content-between pe-2">
                        <div>
                            <p class="mb-1">@k.assignmentType</p>
                            <p>@k.teacher.subject</p>
                        </div>

                        <h2>@k.markPercentage Marks</h2>
                    </div>
                </button>
                    }

                }
                
               
            </div>
                    }
                    else
                    {
                        <div class="row py-2" style="color:gray"><h5 style="text-align:center">No Assignments Yet</h5></div>
                    }
        </div>

<style>
    .title-button {
    border-top: 4px solid #00008b;
    border-radius:5px 5px 0px 0px;
}
</style>